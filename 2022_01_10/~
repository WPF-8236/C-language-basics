#include <stdio.h>
//字符串就是一串字符----用双引号括起来的一串字符串 
int main(){
	"abdc";
	char arr[] = "Hello FH-QY";//字符数组 - 数组是一组相同类型的元素
	 //字符串在结尾的位置隐藏了一个\0的字符,\0为字符串的结束标志 
 	char arr1[] = "abc";
	char arr2[] = {'a','b','c'};
 	printf("%s\n",arr1);
 	printf("%s\n",arr2);
	 return 0;	 
} 
